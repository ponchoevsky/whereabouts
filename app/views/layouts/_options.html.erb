<%= link_to edit_report_path(@report), :class => "btn btn-default btn-sm" do %>
	<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
<% end %>

<%= link_to report_path(@report), method: :delete, data: {confirm: "¿Estás seguro?"}, :class => "btn btn-default btn-sm" do %>
	<span class="glyphicon glyphicon-remove" aria-hidden="true"></span> Eliminar
<% end %>

<%= link_to report_path(@report, "report[found]" => !@report.found), :method => :put, :class => "btn btn-default btn-sm" do %>
	<span class="glyphicon glyphicon-ok" aria-hidden="true"></span> Encontrado
<% end %>